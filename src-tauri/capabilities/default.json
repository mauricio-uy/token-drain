{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the rail window",
  "windows": ["rail"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
